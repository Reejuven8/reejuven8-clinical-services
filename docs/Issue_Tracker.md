# NineMo Backend — Issue Tracker

> Log every problem hit during development: build failures, environment quirks,
> design mismatches, bugs. One row per issue. Update **immediately** when an issue
> is found or resolved. Severity: 🔴 High · 🟡 Medium · 🟢 Low

| ID | Date | Severity | Area | Issue | Root Cause | Resolution | Status |
|---|---|---|---|---|---|---|---|
| IS-001 | 2026-06 | 🔴 | Build | Compilation fails on Java 26 with Lombok errors | Spring Boot 3.3.5 bundles Lombok 1.18.34 — incompatible with Java 26 | Pin `<lombok.version>1.18.38</lombok.version>` in parent POM | ✅ RESOLVED |
| IS-002 | 2026-06 | 🔴 | Tests | Mockito fails to mock on Java 26 (Byte Buddy unsupported class version) | Byte Buddy needs experimental flag for Java 26 | Surefire `argLine: -Dnet.bytebuddy.experimental=true` (global, parent POM) | ✅ RESOLVED |
| IS-003 | 2026-06 | 🟢 | notification-service | Compile error in `NotificationOrchestrator` — variable `log` clashes | Lombok `@Slf4j` generates field `log`; local var shadowed it | Renamed local var to `entry` | ✅ RESOLVED |
| IS-004 | 2026-06 | 🟢 | api-gateway | Planned "add rate limiting" ticket found already implemented | `default-filters` already carried `RequestRateLimiter` (100/150) | No action; ticket closed as already-done | ✅ RESOLVED |
| IS-005 | 2026-06 | 🟡 | clinical-service | DevelopmentalMilestoneService plan didn't match document model | Plan assumed `Map<String,Boolean>` milestones; actual model is `List<Map<String,Object>>` | Service iterates list matching `name` key, mutates `achieved` | ✅ RESOLVED |
| IS-006 | 2026-07-15 | 🟡 | docs | Backend_Architecture.md listed nonexistent endpoints (`/abha/init`, `/consent/status`, `/callback/abdm`, health-data paths) | Doc written from plan, not re-audited against controllers | §9/§10/§12 corrected against actual `@RequestMapping`s | ✅ RESOLVED |
| IS-007 | 2026-07-15 | 🟡 | community-service | STOMP `/ws/**` is permitAll — CONNECT accepts unauthenticated clients; senderId trusted from payload | WebSocket auth never implemented | Fixed via NM-B-161: `StompAuthChannelInterceptor` validates Bearer on CONNECT, requires principal on SEND/SUBSCRIBE; senderId now from JWT principal (payload field removed) | ✅ RESOLVED |
| IS-008 | 2026-07-15 | 🟡 | notification-service | Push notifications have no device-token registry — FCM sends can't target real devices | Endpoint never designed; mobile app didn't exist yet | Fixed via NM-B-162: `device_tokens` table + `POST/DELETE /api/v1/notifications/devices`; orchestrator fans out to all registered devices per user | ✅ RESOLVED |
| IS-009 | 2026-07-15 | 🟢 | API consistency | Response envelope inconsistent: Timeline/SummaryCard/Diet return raw DTOs, rest wrap in `ApiResponse<T>` | Grew organically per phase | Fixed via NM-B-165: 8 clinical controllers wrapped w/ concrete generics | ✅ RESOLVED |
| IS-010 | 2026-07-15 | 🟢 | Observability | correlationId stamped by publishers but lost at consumers (not restored to MDC) | Only publisher half implemented (NM-B-157) | Fixed via NM-B-163: `CorrelationMdc.restore()` (common-lib) in all 6 Java listeners | ✅ RESOLVED |
| IS-012 | 2026-07-16 | 🟡 | ai-parsing-service | correlationId chain silently broken mid-pipeline — Python consumer received it in `DocumentUploadMessage` but never forwarded to `DocumentParsedEvent`, so downstream Kafka consumers correlated nothing | Python pipeline written before correlation-ID work; no cross-language contract check | `pipeline.run()` extracts + structlog-binds `correlationId`; `publish_parsed_document()` emits it in the event JSON (deserializes into `BaseEvent.correlationId`) | ✅ RESOLVED |
| IS-013 | 2026-07-16 | 🟢 | Build | `mvn compile -pl services/X` failed with "cannot find symbol CorrelationMdc" | New common-lib class existed in-reactor only; local `~/.m2` had stale common-lib JAR — `-pl` without `-am` resolves from repo | `mvn install -pl common-lib` after any common-lib change (or always compile with `-am`) | ✅ RESOLVED |
| IS-014 | 2026-07-16 | 🔴 | contract mismatch | RN frontend would break at runtime on Timeline/SummaryCard/Vitals/Growth/Vaccination — services unwrap `res.data.data` (envelope) but backend returned raw DTOs on those endpoints. Latent since frontend build; would 100% fail on first launch | Frontend written to target contract; backend never normalized; no contract test (NM-B-172 deferred) | Backend normalized via NM-B-165 — envelope everywhere; contract now matches. Testcontainers/contract tests would have caught this (see NM-B-153/172) | ✅ RESOLVED |
| IS-015 | 2026-07-16 | 🔴 | identity tests | Both identity unit test files (`JwtTokenProviderTest`, `RsaEncryptionServiceTest`) never compiled — written against invented APIs (`generateToken(UUID,String)`, `encrypt(String)`, `ReflectionTestUtils` fields that don't exist). NM-B-151 was marked ✅ without ever running `mvn test` on the module | Tests authored from the plan doc, not the real classes; per-module test run skipped | Rewrote both against real signatures — `JwtTokenProvider(secret, expiryMs)` ctor, `encrypt(plaintext, base64PublicKey)` w/ generated keypair + decrypt round-trip. 11 tests green | ✅ RESOLVED |
| IS-016 | 2026-07-16 | 🔴 | JPA / PostgreSQL | **All inserts into PG-enum-typed columns fail** (`column "delivery_type" is of type delivery_type but expression is of type character varying`). Hits 11 entities / 12 fields across identity (users.role!), clinical (pregnancy_profiles, vaccination_records, …), notification (notification_logs) — registration, onboarding, and notification logging were all broken in any real PG environment. Unit tests (mocked repos) never touched it; found by NM-B-153 integration test | Hibernate 6 binds `@Enumerated(STRING)` as VARCHAR; PG native enum columns reject without explicit cast | `@JdbcTypeCode(SqlTypes.NAMED_ENUM)` added to all 12 enum fields; full reactor test suite green vs real Postgres | ✅ RESOLVED |
| IS-017 | 2026-07-16 | 🟡 | local env | Testcontainers couldn't reach Docker: OrbStack daemon rejects API < 1.40; Testcontainers pins client API 1.32 (even at 1.21.3) | OrbStack raised min Docker API version; testcontainers-java hardcodes 1.32 | Two-part: bumped Testcontainers 1.20.1→1.21.3 (parent pom) + set `~/.orbstack/config/docker.json` → `{"min-api-version": "1.24"}` + engine restart. CI (docker on ubuntu) unaffected | ✅ RESOLVED |
| IS-018 | 2026-07-17 | 🔴 | clinical-service | **No endpoint to create a pregnancy profile** — every clinical feature (timeline, symptoms, triage, summary card) requires an active `pregnancy_profiles` row, but there is no `POST` to create one; onboarding is impossible end-to-end. Found while writing `UI_Design.md` (P4 Onboarding); integration test masked it by inserting via repository | Onboarding controller never planned in any phase; FRD Pillar 1 "Onboarding & Configuration Engine" mapped to entities only | Resolved 2026-08-11 by NM-B-167: `PregnancyProfileController` — `POST /api/v1/ninemo/profiles/pregnancy` (X-User-Id scoped, 201) computes EDD via the LMP/Ultrasound/IVF strategies and baseline BMI, stores risk flags as JSONB, 409s on a second active profile; `GET` returns the active profile, 404 when onboarding is not done. `UltrasoundCalculationStrategy` was an unimplemented stub — now implemented as `scanDate + (280 - gestationalAgeAtScan)`. The six services that resolve the active profile now throw `ResourceNotFoundException` (404) instead of `IllegalStateException` (500), so the client can tell "not onboarded" from "server broke". Verified live: onboarding → timeline 200 | ✅ RESOLVED |
| IS-011 | 2026-07-15 | 🟢 | health-data-service | Planned SSE route `GET /files/{fileId}/events` unusable — s3Key contains `/` (`patients/{id}/documents/{uuid}/{name}`), breaks path matching. Same latent flaw exists in `GET /files/{s3Key}/download` | Key format chosen before routes | SSE shipped as `GET /files/events?s3Key=` (query param). Download endpoint fix deferred — fold into NM-B-165 API-consistency pass | ✅ RESOLVED |
| IS-019 | 2026-07-19 | 🔴 | ninemo-mobile / shared | `krossbow-websocket-darwin` (used in `shared/build.gradle.kts` iosMain, F0 scaffold) is not and never was a published artifact — `iosSimulatorArm64CompileKlibraries` fails dependency resolution, blocking all iOS compilation of `:shared` | F0 wrote the iOS websocket dependency name from memory instead of verifying against Maven Central; current Krossbow ships Darwin support via `krossbow-websocket-ktor` (Ktor's Darwin engine), not a dedicated `-darwin` artifact | Swapped `libs.versions.toml` + `shared/build.gradle.kts` iosMain to `krossbow-websocket-ktor:9.1.0` (confirmed published). Android side (`krossbow-websocket-okhttp`) was already correct | ✅ RESOLVED |
| IS-020 | 2026-07-19 | 🟡 | ninemo-mobile / shared | `AuthModels.kt` (F0 scaffold) invented request/response field names instead of mirroring `identity-abha-service`'s actual DTOs — `LoginRequest(phoneNumber, password)` when the backend has no password field at all (OTP-only login: `phoneNumber, otp`); `RegisterRequest` missing required `firstName/lastName/dateOfBirth/biologicalSex`; `TokenResponse` claimed `userId/role` fields the backend never returns (JWT claims only); ABHA DTOs used wrong field names (`aadhaarOrMobile` vs `mobileNumber`, `otp` vs `encryptedOtp`, `abhaAddress` vs `preferredAddress`). `SessionStore.save()`/`AndroidSessionStore`/`IosSessionStore` all assumed `tokens.userId`/`tokens.role` existed | F0 built these DTOs from `UI_Design.md` prose descriptions, not by reading `AuthController`/`AbhaController`/their DTO classes directly | Rewrote `AuthModels.kt` to mirror the backend exactly (verified via source read); added `session/JwtDecoder.kt` (decodes `sub`/`role` claims from the access token) since the real `TokenResponse` doesn't carry them; `AbhaRepository.setAddress()` now sends the required `X-User-Id` header | ✅ RESOLVED |
| IS-021 | 2026-07-19 | 🟡 | ninemo-mobile / shared | Koin 4.0.1's `viewModelOf`/`org.koin.core.module.dsl.viewModelOf` DSL (used in `SharedModule.kt` for all ViewModels) fails to resolve when compiling `:shared` for iOS targets (`compileKotlinIosSimulatorArm64`) — resolves fine for Android | Koin's multiplatform ViewModel DSL may not yet cover Native targets at 4.0.1, or needs a different artifact than plain `koin-core`. Not investigated further — switching to plain `factory{}` was considered but rejected: it would drop Koin's ViewModel-scoped caching (config-change/nav-backstack survival) on the already-working, tested Android path just to unblock iOS, which is F7 scope | Open — F7 (iOS SwiftUI catch-up) must resolve before any iOS ViewModel wiring; likely a Koin version bump, needs verification against Koin's KMP-ViewModel changelog | 🔄 OPEN |
| IS-022 | 2026-07-21 | 🟡 | ninemo-mobile / shared | F0's `ClinicalModels.kt`/`TimelineModels.kt` stubs were invented from `api.ts`/prose descriptions, not the real `ninemo-clinical-service` DTOs — same pattern as IS-020. Timeline's `BabyDevelopment`/`Milestone`/`DietTip`/`YogaRoutine` were fictional typed classes (backend actually returns untyped `Map<String,Object>` — no fixed schema, CMS-style content); `SymptomLogRequest.symptoms` was `List<String>` (backend: `List<Map>` with `name/category/severity` keys); `SymptomLogResponse` had wrong field names (`gestationalWeekAtLog`→`gestationalWeek`, `triageResult`→`triggeredRules`) and GET history returns a *different, richer* shape than POST response (stub conflated the two); `VitalsLogResponse` was missing 6 real fields and mistyped `measurements`; `SummaryCardResponse` was a near-total fabrication (5 of 8 fields didn't exist backend-side: `pregnancyProfileId`, `edd`, `lastSymptomLog`, `lastKickSession`) | Same root cause as IS-020 — DTOs written from spec prose instead of reading the actual Spring controllers/documents | Rewrote both files against `TimelineController`/`SymptomController`/`VitalsController`/`SummaryCardController` + their DTO/document classes, `SymptomTriageEngine` rule implementations (to recover the real `symptoms[].name`/`vitalsAtLog` key vocabulary — snake_case, e.g. `blood_pressure_systolic`), and `Database_Design.md` §6.2-6.4 for the canonical Mongo schemas. Untyped backend fields kept as `Map<String, JsonElement>` client-side rather than invented fixed classes | ✅ RESOLVED |
| IS-023 | 2026-07-21 | 🟢 | ninemo-mobile / shared | `androidApp` couldn't resolve `JsonElement`/`JsonPrimitive` (used in new Timeline/SummaryCard DTOs) — "Cannot access class" compile errors despite `:shared` having the dependency | `shared/build.gradle.kts` declared `kotlinx-serialization-json` as `implementation`, not `api`, so it didn't propagate transitively to `androidApp` even though the types now appear in `:shared`'s public DTOs | Changed to `api(libs.kotlinx.serialization.json)` in `shared/build.gradle.kts` commonMain | ✅ RESOLVED |
| IS-024 | 2026-07-22 | 🟡 | ninemo-mobile / shared | Third occurrence of F0 DTO invention (same pattern as IS-020/IS-022), this time in kick counter/contraction/diet: kick+contraction "add"/"end" actions are **PUT**, not POST (stub assumed POST); both response DTOs use `sessionId` not `id`, and are missing `gestationalWeek`; kick's `durationTo10KicksMinutes` is `Int` not `Double`; kick's `kickTimestamps`/`active` fields don't exist on the wire at all (infer "active" from `sessionEnd == null`); contraction's `contractions[]` is untyped `Map<String,Object>` (no `endTime` ever populated despite the DB design doc showing one) and averages are non-nullable `Int` not `Double`; diet's `SafetyRating` enum was `SAFE/MODERATE/AVOID/UNSAFE` — real server enum is only `SAFE/CAUTION/AVOID` (3 values, `MODERATE`→`CAUTION`, no `UNSAFE`); diet's `description`→`medicalReasoning`, no `id`/`trimesterTags`/`categories` on the response despite existing on the JPA entity. Also: **no GET history endpoint exists** for kick-counter or contraction sessions at all — the repository methods are dead code backend-side | Same root cause each time — DTOs ported from `api.ts`/prose instead of the real controllers | Rewrote `ClinicalModels.kt` (kick+contraction sections) and `DietModels.kt` against `KickCounterController`/`ContractionController`/`DietController` + their real DTOs/documents/entities and `ContractionService`'s actual labor-pattern math (`avgInterval<=300 && avgDuration>=60`, alert = that `&& week<37` — note the code's "≥1 hour sustained" comment is aspirational, not actually implemented) | ✅ RESOLVED |
| IS-025 | 2026-07-22 | 🔴 | ninemo-mobile / shared | `ApiRoutes.fileDownload(key)`/`fileEvents(recordId)` (F0 scaffold, never previously called by any repo) built **path-segment** URLs (`/health/files/{key}/download`, `/health/files/{recordId}/events`) — real backend only accepts **query-param** form (`/health/files/download?s3Key=`, `/health/files/events?s3Key=`, per IS-011's fix on the backend side). Calling the old helpers as written would 404 against the real backend — a live bug, not just historical | F0 wrote these route helpers without checking IS-011's backend fix had changed the route shape from path- to query-param-based | Replaced with `FILE_DOWNLOAD`/`FILE_EVENTS` base-path constants; callers (`FilesRepository`) attach `s3Key` via Ktor's `parameter()` | ✅ RESOLVED |
| IS-027 | 2026-07-30 | 🔴 | ninemo-clinical-service | **No `X-User-Id` and no ownership check on ANY of the four child-mode controllers** (Growth, Vaccination, Milestone, ModeTransition) — every one does a bare `findById(childId)`/`findById(pregnancyProfileId)` with zero verification the child/pregnancy belongs to the caller. Any authenticated user who knows/guesses a `childId` UUID can read another child's growth data, vaccination schedule, milestones, and even mark their vaccinations completed. Diverges from every other clinical controller (timeline/symptoms/vitals/kick/contraction all take + scope by `X-User-Id`). Found during `ninemo-mobile` F5 build | Child-mode controllers written without the gateway-injected `X-User-Id` scoping the rest of the service uses | Resolved 2026-08-11 by NM-B-169: all four controllers now take `@RequestHeader("X-User-Id")`, and every child-scoped service call routes through the new `ChildAccessGuard` (`requireOwnedChild` / `requireOwnedPregnancy`) → 403 `FORBIDDEN` for a foreign id, 404 for an unknown one. Records reached indirectly are resolved back to their owner first: `markCompleted` via `VaccinationRecord.childId`, `markMilestone` via the Mongo document's `child_id`. `ChildAccessGuardTest` (5 tests) + a real-PG integration test cover it; verified live that a stranger gets 403 on children/growth/vaccinations/milestones while the parent still gets 200 | ✅ RESOLVED |
| IS-028 | 2026-07-30 | 🔴 | ninemo-clinical-service | **No child-listing / child-get endpoint and no active-pregnancy-get endpoint exist** — `ChildProfileRepository.findByParentUserIdAndIsActiveTrue` and `PregnancyProfileRepository.findByUserIdAndIsActiveTrue` exist but no controller exposes them. Consequence: the mobile app can only ever obtain a `childId` **once**, inline from the one-time `POST transition-to-postnatal` response. After reinstall / new device / cache loss there is NO way to recover it — every child-mode screen (P6/P17/P18/P19) is then unreachable. Same class of gap as the missing kick/contraction session-list endpoints (prior passes). Also blocks obtaining `pregnancyProfileId` to even call the transition | Child-profile and pregnancy-profile read endpoints were never built (compounds NM-B-167) | Resolved 2026-08-11 by NM-B-169: new `ChildProfileController` — `GET /ninemo/children` (list, X-User-Id scoped), `GET /ninemo/children/{childId}`, `PATCH /ninemo/children/{childId}`; plus `GET /ninemo/profiles/pregnancy` from NM-B-167 for the active pregnancy. `ChildProfileResponse` carries a server-computed `ageInMonths`. childId is now recoverable after a reinstall, so mobile's encrypted-storage cache is a convenience rather than the only copy | ✅ RESOLVED |
| IS-029 | 2026-07-30 | 🟡 | ninemo-mobile / shared | Fifth occurrence of F0 DTO invention (auth→020, clinical→022, tools/diet→024, locker/consent→026), this time child mode — the biggest structural divergences yet: **ModeTransition request body is entirely fictional** (endpoint takes NO body; baby name/sex/weight/deliveryType all silently dropped — server hardcodes `childName=null`, `sex=OTHER`, `weight=null`, `deliveryDate=now`), and its response is the bare `ChildProfile` entity not the stubbed 3-field composite (`vaccinationScheduleCreated`/`pregnancyProfileLocked` flags don't exist though the side effects happen); **milestones are `List<{name,achieved}>` not `Map<String,Boolean>`**, and there is **no `DelayRisk` enum** (delay-risk is a string in `alertFlags`, "DEVELOPMENTAL_DELAY_RISK", binary); **mark-completed (vaccination) and achieve (milestone) take QUERY PARAMS not JSON bodies**, both PUT, and milestone param is `milestoneName` (exact text) not `milestoneKey`; growth map keys are snake_case WHO indicators (`weight_for_age` etc, no `weight_for_height`), percentiles are `Int` not `Double`, `ageInMonths` is client-supplied+required (not server-derived), and both Growth+Vaccination controllers return the **raw document/entity** (the `*Response` DTOs are dead code); vaccination has no `overdue` boolean (derive from `status==OVERDUE`), OPV birth dose is `doseNumber 0` | Same root cause every time — DTOs ported from `api.ts`/prose instead of reading the real controllers/services/documents | Rewrote `PediatricModels.kt` fully + removed obsolete ChildProfile/ModeTransition DTOs from `ProfileModels.kt` + dropped the `DelayRisk` enum from `Common.kt`. Repos send query params for the PUT actions; ModeTransition sends no body. Milestone month list (2,4,6,9,12,18,24,36,48,60) and growth Z-key names sourced from the real service code | ✅ RESOLVED |
| IS-026 | 2026-07-22 | 🟡 | ninemo-mobile / shared | Fourth occurrence of F0 DTO invention (auth→IS-020, clinical→IS-022, kick/contraction/diet→IS-024), this time in health records/files/consent: `HealthRecordResponse` invented a flat `recordType`/`title`/`fileKey` model — real backend returns raw FHIR resource documents (`resourceType`/`sourceFileS3Url`, untyped `code`/`valueQuantity` maps, no `title` field anywhere); `HealthRecordDetailResponse`/`ParsedObservation`/`insight` don't exist at all — detail endpoint returns the *same* flat FHIR shape as the list endpoint, no nested observations; list endpoint returns Spring Data's raw `Page<T>` JSON shape, not `List` or common-lib's `PagedResponse<T>`; `FileUploadResponse` claimed a `recordId` that doesn't exist (upload never synchronously creates a record — async pipeline, no correlation id ever surfaced) and used `fileKey`/`parseStatus` instead of real `s3Key`/`status`; `PresignedUrlResponse{url,expiresInSeconds}` doesn't match reality — download endpoint returns a bare `ApiResponse<String>`; SSE parse-status payload is a **bare string** under a named `parse-progress` event, not JSON, and only `PROCESSING`/`PARSED` exist (no `FAILED` path wired up backend-side); `ConsentGrantRequest.durationDays` doesn't exist — server takes an absolute `expiresAt` Instant, client must compute it; `ConsentResponse.doctorName`/`status` don't exist (`consentStatus` is the real field name, no doctor-name enrichment anywhere, and no doctor search/lookup endpoint exists in identity-abha-service at all — matches the design mockup's manual "doctor's NineMo ID" entry, so no product gap, just a stub-vs-reality one); `consentStatus` never auto-transitions to `EXPIRED` server-side (no scheduled job) | Same root cause each time | Rewrote the health-records/files/consent sections of `ProfileModels.kt` against `HealthRecordController`/`FileUploadController`/`ConsentController` + `FhirResourceResponse`/`FileUploadResponse`/`ConsentRequest`/`ConsentResponse`/`ParseProgressService`. Added `SpringPage<T>` to model the real list envelope. Untyped FHIR maps kept as `Map<String,JsonElement>` client-side. Client renders EXPIRED by comparing `expiresAt` itself (presentation of already-provided data, not new clinical logic) | ✅ RESOLVED |

| IS-030 | 2026-08-02 | 🟡 | ninemo-mobile / shared | Sixth occurrence of F0 DTO invention (auth→020, clinical→022, tools/diet→024, locker/consent→026, child→029), this time community (F6): `CommunityModels.kt` diverged from `ninemo-community-service` — `ChatMessageResponse` had a required non-null `isDeleted` field that **does not exist on the response DTO** (only on the entity; would crash deserialization on every message since `ignoreUnknownKeys` can't supply a missing required field); `ClubResponse` had a required `members` list (absent from the DTO — present only on the raw entity `POST /clubs/join` returns) and was **missing `isMember`**; `SendMessageRequest` carried a `senderId` the backend explicitly ignores (identity from STOMP principal). **Plus a serialization gotcha**: backend Lombok boolean getters serialize is-prefix-stripped by Jackson (no global `PropertyNamingStrategy`), so `isMember`→`"member"` and `Channel.isDefault`→`"default"` on the wire — mobile needs `@SerialName`. Also `ApiRoutes.deleteMessage(messageId)` built the wrong path (`/community/messages/{id}`; real route is `/clubs/{clubId}/channels/{channelId}/messages/{id}`) — now unused, repo builds the correct path from `clubMessages()` | Same root cause every time — DTOs ported from `api.ts`/prose instead of reading the real controllers/DTOs/entities | Rewrote `CommunityModels.kt` against `ChatController`/`ChatRestController`/`ClubController`/`ContentController` + their DTOs and the `DueDateClub`/`ChatMessage`/`ContentArticle` entities. `members`+`isMember` both optional (join returns entity shape, list/get return DTO shape — one model absorbs both). `@SerialName("member")`/`@SerialName("default")` for the Jackson boolean names. STOMP live chat via Krossbow (`ChatSocketClient`) over SockJS raw-ws transport (`:8086/ws/connect/websocket`), bypassing the gateway (STOMP self-validates the JWT) | ✅ RESOLVED |
| IS-031 | 2026-08-02 | 🟢 | ninemo-community-service | A member's own club **alias is not retrievable after join** — `ClubResponse` DTO (returned by `GET /clubs`, `GET /clubs/{id}`) omits the `members` list entirely, so on reinstall / new device the app can't recover the alias the user chose, and `SendMessageRequest.senderAlias` is required. Only the one-time `POST /clubs/join` response (raw entity) carries it. Same class of gap as the childId one-time-only issue (IS-028) | `ClubResponse` DTO surfaces only aggregate `memberCount`, not the caller's membership row | Mobile mitigates: alias seeded from the join-time route arg, kept user-editable, defaults to "Member". Backend fix tracked as NM-B-173 | 🔄 OPEN |

| IS-032 | 2026-08-02 | 🔴 | api-gateway | **Gateway jar never boots** — `common-lib` declares `spring-boot-starter-web`, which drags `spring-webmvc` + `tomcat-embed-core` onto the (reactive/WebFlux) Spring Cloud Gateway classpath. `GatewayClassPathWarningAutoConfiguration$SpringMvcFoundOnClasspathConfiguration` throws on startup → context init cancelled → process exits. Means the gateway has never actually run from its built artifact (the "100% done" reactor tests never `java -jar`'d it). Found during first live F6 device test | common-lib is a shared kernel used by both servlet services (need starter-web) and the reactive gateway (must not have it); no exclusion was set | Excluded `spring-boot-starter-web` from the gateway's `common-lib` dependency (gateway only uses common-lib POJOs — ApiResponse/JwtClaims — not servlet types). Rebuilt; gateway boots + routes. Proper long-term fix: make starter-web `optional` in common-lib and have each servlet service declare it | ✅ RESOLVED |
| IS-033 | 2026-08-02 | 🟡 | infra / mongo | `infrastructure/init-scripts/mongo-init.js` calls `db.auth('reejuven8','dev_password')` against the `reejuven8` db, but the `MONGO_INITDB_ROOT_USERNAME` user is created in the `admin` db → `UserNotFound` thrown inside the init script → mongod entrypoint aborts → `reejuven8-mongodb` exits (1). Community + clinical (Mongo-backed) can't start. Only surfaced now because the full stack was never brought up end-to-end before | Init scripts already run authenticated as the root user; the explicit `db.auth()` is both unnecessary and wrong (wrong db) | Removed the `db.auth()` line. Recreated the container + volume; init completes (11 collections), health green | ✅ RESOLVED |
| IS-034 | 2026-08-02 | 🔴 | ninemo-community-service (+ ninemo-clinical-service) | **Every REST call 403s.** `SecurityConfig` does `.anyRequest().authenticated()` but the service configures **no authentication mechanism** (no `oauth2ResourceServer`/JWT filter, no X-User-Id trust) — so the SecurityContext is always empty and all `/api/v1/**` requests are rejected 403. The intended model is gateway-as-auth-boundary (gateway validates JWT + injects `X-User-Id`, controllers read it), so these paths must be `permitAll`. `ninemo-clinical-service` has the identical bug. Never caught because integration tests use mock auth / MockMvc that bypasses the filter chain | Copy-paste `.authenticated()` security config without a matching resource-server/JWT filter | Community: `.anyRequest().permitAll()` (trust gateway-injected identity; STOMP still authed by `StompAuthChannelInterceptor`). Clinical fixed the same way 2026-08-11 (NM-B-174) — `permitAll` + actuator/swagger matchers, with per-resource ownership now enforced in the service layer by `ChildAccessGuard` instead of by an empty SecurityContext. Verified live: every clinical endpoint returns real data instead of 403 | ✅ RESOLVED |
| IS-035 | 2026-08-02 | 🔴 | api-gateway | **Community REST unreachable via gateway** — route ordering. `ninemo-clinical-service` route (`Path=/api/v1/ninemo/**`, with a CircuitBreaker+fallback) is defined **before** `ninemo-community-service-rest` (`Path=/api/v1/ninemo/community/**`). Spring Cloud Gateway matches in definition order, first-match-wins, so `/api/v1/ninemo/community/**` is swallowed by the clinical route → forwarded to clinical (:8084) → (clinical down →) circuit-breaker `503 SERVICE_UNAVAILABLE`. Even with clinical up it would 404. App F6 community screens all failed with 503 | Broad clinical path predicate defined before the more-specific community path, no explicit `order` | Added `order: -1` to `ninemo-community-service-rest` so the specific route wins. Rebuilt gateway; full chain (device→gateway→community `/clubs`) → 200 | ✅ RESOLVED |
| IS-036 | 2026-08-02 | 🔴 | ninemo-mobile / shared | **No authenticated request works after login (same app session).** Ktor's Bearer `Auth` plugin caches the `loadTokens` result on first use. The pre-login requests (otp/send, login) run while `session.tokens()` is null → the plugin caches a null token and never reloads after `session.save(tokens)`. Result: every authenticated request (`/profiles/pregnancy`, `/symptoms`, `/clubs`, …) goes out with **no `Authorization` header** → gateway 401 (and the plugin's 401→refresh with a null refresh token then `session.clear()`s, logging the user out). Captured via a loopback logging proxy: 0 Authorization headers on all authed requests. Invisible until now — the app had never run against a live backend | Ktor `BearerAuthProvider` token caching not invalidated after an out-of-band login | `AuthRepository.login()`/`register()` now call `client.authProviders.filterIsInstance<BearerAuthProvider>().firstOrNull()?.clearToken()` after `session.save()`, forcing a reload of the freshly-saved token. Verified: `/clubs` now carries the bearer + returns 200. (A fresh app start with a persisted session was already fine — the bug is login within a process that had cached null) | ✅ RESOLVED |

| IS-037 | 2026-08-02 | 🟢 | ninemo-mobile / UI | Screen-to-screen navigation shows a fade in / fade out transition — the Compose Navigation `NavHost` default (`fadeIn`/`fadeOut`). Feels sluggish; want instant (or non-fade) screen changes across the app | `NavHost`/`composable` in `NineMoNavHost.kt` use the default fade transitions; none overridden | Open — set `enterTransition = { EnterTransition.None }` + `exitTransition = { ExitTransition.None }` (and `popEnter`/`popExit`) on the `NavHost`(s) in `NineMoNavHost.kt`, or per-`composable`. Applies to both the auth stack and the MainShell NavHost | 🔄 OPEN |

| IS-038 | 2026-08-02 | 🔴 | notification-service | **Service cannot start on a clean database.** `V1__create_notification_logs.sql` declares columns of PG enum types `notification_channel` / `notification_status` but **nothing ever creates those types** — not the migration (0 `CREATE TYPE` statements in the whole service, vs 7 in clinical and 8 in identity, which are self-sufficient) and not `infrastructure/init-scripts/postgres-init.sql` (which only does `CREATE DATABASE`). Flyway fails with `ERROR: type "notification_channel" does not exist` → `flywayInitializer` bean creation fails → app exits. Found while adding standalone run configs and smoke-testing each service | The two enum types were assumed to be created by the shared postgres init script, but that script never defined any types; notification was the only service that didn't create its own | Prepended guarded `DO $$ ... EXCEPTION WHEN duplicate_object $$` blocks creating both types to V1 (PostgreSQL has no `CREATE TYPE IF NOT EXISTS`); values mirror `NotificationChannel` (WHATSAPP/SMS/PUSH/EMAIL) and `NotificationStatus` (PENDING/SENT/DELIVERED/FAILED/SKIPPED). Safe to edit V1 in place — it had never applied successfully anywhere. Verified: service boots, `/actuator/health` 200 | ✅ RESOLVED |

| IS-039 | 2026-08-11 | 🔴 | notification-service + ninemo-clinical-service | **Two services share one PostgreSQL database, so their Flyway histories collide and one of them always fails to boot.** Both `application.yml`s pointed at `${DB_NINEMO_URL:...reejuven8_ninemo}`. Each runs its own `V1__`/`V2__` migrations into the single `flyway_schema_history` in that database, so whichever service migrates first wins and the other dies with `Migration checksum mismatch for migration version 1`. Observed in both directions: notification had claimed the history (clinical wouldn't start), and after clearing it clinical claimed the history (notification wouldn't start). Also violates the one-database-per-owning-service rule in CLAUDE.md. Found while smoke-testing NM-B-167/169 against a live stack — IS-038's fix had only ever been verified with clinical *not* running | Both services were given the same `DB_NINEMO_URL` default; nothing ever ran the two together, so the collision stayed latent. `postgres-init.sql` only created `reejuven8_identity` + `reejuven8_ninemo`, so no third database existed to point at | Gave notification-service its own database: new `DB_NOTIFICATION_URL` → `reejuven8_notification`; added `CREATE DATABASE reejuven8_notification` + grant + `uuid-ossp` to `infrastructure/init-scripts/postgres-init.sql`; fixed `docker-compose.yml`, whose `DB_URL`/`DB_USER`/`DB_PASS` names never matched the `${DB_NINEMO_URL}`/`${DB_USERNAME}`/`${DB_PASSWORD}` placeholders in either service (so compose could never have configured them at all). Existing dev DB repaired by hand: created the new database, dropped notification's 2 empty tables + history + enum types out of `reejuven8_ninemo`. Verified: clinical applied V1–V8 in `reejuven8_ninemo`, notification applied V1–V2 in `reejuven8_notification`, both `/actuator/health` 200 simultaneously | ✅ RESOLVED |

---

## Template (copy for new issues)

```
| IS-0XX | YYYY-MM-DD | 🔴/🟡/🟢 | <service/area> | <what broke / what's wrong> | <why> | <fix or ticket ref> | 🔄 OPEN |
```
