apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-data-service
  namespace: ninemo
spec:
  replicas: 2
  selector:
    matchLabels: { app: health-data-service }
  template:
    metadata:
      labels: { app: health-data-service }
    spec:
      containers:
        - name: health-data-service
          image: ninemo/health-data-service:latest
          ports: [{ containerPort: 8082 }]
          envFrom:
            - configMapRef: { name: ninemo-config }
            - secretRef: { name: ninemo-secrets }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8082 }
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8082 }
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits: { cpu: 1000m, memory: 2Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: health-data-service
  namespace: ninemo
spec:
  selector: { app: health-data-service }
  ports: [{ port: 8082, targetPort: 8082 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: health-data-service-hpa
  namespace: ninemo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: health-data-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
