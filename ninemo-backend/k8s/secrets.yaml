# Template — replace placeholder values before applying to any environment.
# Store actual secrets in AWS Secrets Manager or Vault; use External Secrets Operator to sync.
apiVersion: v1
kind: Secret
metadata:
  name: ninemo-secrets
  namespace: ninemo
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  MONGODB_PASSWORD: "REPLACE_ME"
  RABBITMQ_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_MIN_256_BIT_SECRET"
  AWS_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"
  AWS_S3_BUCKET: "REPLACE_ME"
  TWILIO_ACCOUNT_SID: "REPLACE_ME"
  TWILIO_AUTH_TOKEN: "REPLACE_ME"
  TWILIO_WHATSAPP_NUMBER: "REPLACE_ME"
  FIREBASE_SERVICE_ACCOUNT_JSON: "REPLACE_ME"
